resource-changed.kafka.bootstrap-servers=${KAFKA_STREAMING_BROKER_ADDR:localhost:9092}
resource-changed.consumer.topic=${STREAM_COMPANY_OFFICERS_TOPIC:stream-company-officers}
resource-changed.consumer.concurrency=${RESOURCE_CHANGED_CONCURRENT_LISTENER_INSTANCES:1}

officer-merge.kafka.bootstrap-servers=${KAFKA3_BROKER_ADDR:localhost:9092}
officer-merge.consumer.topic=${OFFICER_MERGE_TOPIC:officer-merge}
officer-merge.consumer.concurrency=${OFFICER_MERGE_CONCURRENT_LISTENER_INSTANCES:1}

consumer.group-id=${GROUP_ID:officers-search-consumer}
consumer.max-attempts=${MAX_ATTEMPTS:5}
consumer.backoff-delay=${BACKOFF_DELAY:1000}

logger.namespace=officers-search-consumer

management.endpoints.enabled-by-default=false
management.endpoints.web.base-path=/
management.endpoints.web.path-mapping.health=healthcheck
management.endpoint.health.show-details=never
management.endpoint.health.enabled=true

api.api-url=${API_URL:localhost}
api.api-key=${CHS_API_KEY:chsApiKey}

server.port=${SERVER_PORT:8081}